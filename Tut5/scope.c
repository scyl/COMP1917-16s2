#include <stdio.h>
#include <stdlib.h>

int sum (int first, int second);

int main(int argc, char* argv[]){
    int num1 = 3;
    int num2 = 5;
    int wow= 2;
    printf (wow);
    if (1) {
        int num = 6;
        num2 = 6;
    }
    num = 3;
    while (i<n)
    sum(num1, num2);
    sumNum = 2;
    return EXIT_SUCCESS;
}

int sum (int first, int second) {
    int sumNum = first + second;
    int i;
    while(i<x)
        
        return i;
}